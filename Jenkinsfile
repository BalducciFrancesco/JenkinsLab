pipeline {
  agent any
  
  stages {
    stage('First stage') {
      steps {
        touch file1.txt
      }
    }
    stage('Second stage') {
      steps {
        touch file2.txt
      }
    }
  }
}
